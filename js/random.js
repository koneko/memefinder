var main = document.querySelector('.content')

function gotoRandom() {
    if (localStorage.page != "Random") {} else {
        console.log('Random page active.')
        // document.body.appendChild(document.createElement('h1').innerText = "hello")
    }
}